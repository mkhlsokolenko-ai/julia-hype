# Скопируй в .env и впиши anon public ключ (из Supabase → Settings → API → anon public,
# или из Vercel env существующего проекта). .env в .gitignore — в репозиторий не попадёт.
VITE_SUPABASE_URL=https://zzwwobiypfkwaawxlyud.supabase.co
VITE_SUPABASE_ANON_KEY=
